/target
/models
NOTE.md
/.claude-memory/

# Локальные env-переменные для сборочного pipeline (HTTP_PROXY и т.п.).
# Шаблон лежит в .env.example. См. начало scripts/build-deb.sh — он source'ит
# .env.local при наличии, чтобы локальная сборка подхватывала прокси/токены/
# другие персональные настройки без правки коммитимых файлов.
.env.local

# Авто-качаемые pre-built нативные библиотеки.
# libonnxruntime-noavx.so коммитим (self-build, 25 МБ),
# остальные тащит scripts/prepare-bundled-libs.sh при сборке .deb.
/assets/libs/libonnxruntime-avx2.so
/assets/libs/libvosk.so

# Windows ORT DLL — качается CI-job'ом build-windows-test (Microsoft win-x64),
# в git не коммитим (бинарь ~10 МБ). Лежит рядом с tauri.windows.conf.json
# resources. Локально для dev-сборки положить руками сюда же.
/crates/arcanaglyph-app/libs/onnxruntime.dll

.gemini/

# arcanacodex (auto-managed)
.arcanacodex/
