{
  "name": "arcanaglyph-frontend",
  "version": "1.7.6",
  "private": true,
  "type": "module",
  "description": "ArcanaGlyph UI (Tauri webview). Vite + TypeScript build pipeline. FSD-структура — на следующих итерациях.",
  "scripts": {
    "dev": "vite --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@tauri-apps/api": "~2.10.0"
  }
}
